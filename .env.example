# =============================================================================
# LLM Configuration (Required)
# =============================================================================

# Google API Key for Gemini model access
# You can set this to any LLM you wish
GOOGLE_API_KEY=your-google-api-key-here

# Gemini model to use
LLM_MODEL=gemini-2.5-flash

# =============================================================================
# Embedding Configuration (Required)
# =============================================================================

GOOGLE_API_KEY_EMBEDDING=your-google-api-key-for-embeddings
EMBEDDING_PROVIDER=google
EMBEDDING_MODEL=gemini-embedding-001
EMBEDDING_DIMENSIONS=768

# =============================================================================
# Database Configuration for RAG Database
# =============================================================================

# Supabase project URL
# Format: https://your-project-id.supabase.co
# Get from: Your Supabase project dashboard > Settings > API
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase service role key (NOT the anon key)
# Get from: Your Supabase project dashboard > Settings > API Keys > Service role key
SUPABASE_KEY=your-supabase-service-role-key-here

# Local ChromaDB database path (auto-created if not exists)
CHROMADB_PATH=~/.mrtrix3-agent/chromadb

# =============================================================================
# Logging Configuration
# =============================================================================
# - When true: All logs are saved to monitoring/logs/ directory only
# - When false: No logging (default)
COLLECT_LOGS=false
